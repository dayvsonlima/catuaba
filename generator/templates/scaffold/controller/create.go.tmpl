package {{.Name|toPlural|toSnake}}

import (
	"log"
	"net/http"

	"{{moduleName}}/app/models"
	"{{moduleName}}/database"

	"github.com/gin-gonic/gin"
)

type CreateParams struct {
	{{ range .Params}}{{. | toAttrName}} {{. | toType}} {{. | toJsonBinding}}
	{{ end }}
}

// Create handles POST /{{.Name|toPlural|toSnake}}
func Create(ctx *gin.Context) {
	var params CreateParams
	if err := ctx.ShouldBindJSON(&params); err != nil {
		ctx.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})
		return
	}

	{{.Name|toVarName}} := &models.{{.Name|toModelName}}{
		{{ range .Params}}{{. | toAttrName}}: params.{{. | toAttrName}},
		{{ end }}
	}

	if err := database.DB.Create({{.Name|toVarName}}).Error; err != nil {
		log.Printf("error creating {{.Name|toSnake}}: %v", err)
		ctx.JSON(http.StatusInternalServerError, gin.H{"error": "An unexpected error occurred"})
		return
	}

	ctx.JSON(http.StatusCreated, {{.Name|toVarName}})
}
