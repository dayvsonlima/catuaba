APP_NAME={{.Name}}
APP_PORT=8080

# Database configuration
# Supported drivers: postgres, mysql, sqlite
DB_DRIVER={{.DBDriver}}
DB_HOST={{.DBHost}}
DB_PORT={{.DBPort}}
DB_USER={{.DBUser}}
DB_PASS=your_password_here
DB_NAME={{.DBName}}
DB_SSLMODE=disable

# CORS configuration
CORS_ALLOWED_ORIGIN=*
